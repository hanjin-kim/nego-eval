[project]
name = "nego-eval"
version = "0.1.0"
description = "A negotiation board game where the counterparty is chosen and remembered"
requires-python = ">=3.10"
license = "BUSL-1.1"
license-files = ["LICENSE"]
dependencies = ["httpx>=0.27"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "tests/sim"]
